spring:
  data:
    mongodb:
      database: userAuthDb
      port: 27017
      host: localhost
  application:
    name: user-auth
server:
  port: 9100
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true